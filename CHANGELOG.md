# 1.0.0

- Initial release.
- Server-side sleep voting: when a configurable percentage of players are in bed, awake
  LessSleep clients are asked to accept or decline; the night skips when enough agree.
- Works without any client mods installed (auto-skip when nobody can vote).
- Client prompt with `Y` / `N` keys and `yes` / `no` chat commands.
